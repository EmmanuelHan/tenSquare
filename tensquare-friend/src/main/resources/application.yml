spring:
  profiles:
    active: '@spring.active@'

application:
  version: 1.0.0

# 服务端口号
server:
  port: 9004
  application:
    name: tensquare-friend

mybatis-plus:
  mapper-locations: classpath:mapper/*/**.xml
